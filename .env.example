# 复制为 .env 后按需修改
POSTGRES_USER=castsim
POSTGRES_PASSWORD=castsim_pwd
POSTGRES_DB=castsim

# 宿主机映射端口
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=8080

# 后端启动时是否自动写入演示数据（true/false）
SEED_ON_START=true
